package io.github.derrickmunyole.loandecisioning.workflow.workqueue;

/** {@code RESOLVED} arrives with Epic 4.2's manual retry/resolve action; nothing sets it yet. */
public enum WorkflowTaskStatus {
    OPEN
}
